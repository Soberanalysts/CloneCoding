1. 브라우저 렌더링의 순서
    서버로부터 HTML전달받음
    브라우저엔진이 위에서 아래로 순차적으로 파싱
    파싱한 데이터의 태그와 속성을 트리형태로 메모리에 저장(DOM트리)
    파싱중 CSS링크나 스타일 태그 만나면 CSSOM(CSS Object모델)  트리생성
    렌더트리 생성(DOM트리+CSSOM)
    Reflow(배치) Repaint(색칠)과정을 거쳐 띄우기

2. 파싱중간에 script 태그 만날때?
    해당 script를 로드하고 실행하기위해 파싱중단
    외부스크립트 : 스크립트 로드후 파싱진행
    내부스크립트 : 실행완료시까지 파싱 중단
    async와 defer속성으로 파싱이 미치는 영향 최소화 가능

    